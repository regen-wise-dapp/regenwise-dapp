export interface QuestsRankings {
    id: string;
    nickname: string;
    points: number;
  }
  export interface QuestsStatistics {
    questDate: string;
    duration: string;
    difficulty: string;
    points: number;
  }

  export interface TableColumn {
    id: string;
    title: string;
  }